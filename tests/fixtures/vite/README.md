Copias de `vite.config.*` tal como los publican `laravel/laravel` y los starter
kits oficiales (MIT), tomadas el 2026-09-01. Sirven para comprobar que
`configurar_vite` inserta la configuración de DDEV dentro del bloque `server`
que ya traen, sin crear un segundo bloque.
