{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Escuchar Xdebug (DDEV)",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "hostname": "0.0.0.0",
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}"
            }
        }
    ]
}
